package sec06.exam04.package2;

import sec06.exam04.package1.A;

public class C {
	// 필드
	A a1 = new A(true);
//	A a2 = new A(1); // default 생성자여서 다른 패키지에서는 사용 불가
//	A a3 = new A("문자열"); // private 생성자여서 다른패키지, 다른클래스에서는 사용 불가
}
